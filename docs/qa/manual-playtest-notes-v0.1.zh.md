# 《最后一间教室》v0.1 手动游玩记录

本文记录 P0.1 完整手动游玩 QA 的结果。自动烟测只能确认流程和关键节点存在，不能替代实际手测中的观感、节奏和遮挡问题。

## 当前状态

状态：`[doing]`

最近一次自动验证：

```text
TITLE_SCREEN_SMOKE_TEST_OK
FLOW_SMOKE_TEST_OK
VISUAL_FEEDBACK_SMOKE_TEST_OK
```

自动验证已确认：

- 标题页可加载，项目启动场景正确。
- A1-A12 可按顺序完成。
- 结尾卡、测绘项、完成纸签、目标文案正常。
- 真实触发路径下，拍照取景框/快门闪光和显影层会进入可见状态。

最近一次截图检查：

```text
QA_SCREENSHOT_CAPTURE_OK
```

截图说明：

- 截图由 `scripts/tools/capture_qa_screenshots.gd` 生成到本地 `docs/qa/screenshots/`。
- 截图目录是本地 QA 产物，不纳入 Git。
- 已检查 4K 全屏下标题页和开场对话截图。
- 标题页曾出现“具体”被拆成“具 / 体”的断行问题，已通过加宽标题页正文区域修复。
- 已检查默认 960x540 逻辑视口下标题页和开场对话截图。
- 已补充默认 960x540 逻辑视口下 A2 显影、A5 拍照反馈、A12 终局显影截图检查。
- 尝试生成 1280x540 宽屏截图时，输出仍为 960x540；原因是项目当前使用固定 `viewport_width=960`、`viewport_height=540` 和 `window/stretch/mode="canvas_items"`，脚本截图取到的是逻辑视口，不是操作系统窗口外框。

## 手测要求

手测时不要使用：

- `F6` 强制完成事件。
- `F7` 传送到下一个事件。

允许使用：

- `F3` 查看调试 HUD，但不作为通过流程的必要条件。
- `F4` 只在定位交互问题时打开，正常展示流程应保持关闭。

## 手测路径

1. 标题页按 `E/Enter` 开始。
2. 读完车内开场。
3. 按正常移动和交互完成 A1-A12。
4. 看到结尾卡“测绘完成”。

## 检查项

### UI 与遮挡

状态：`[partial]`

- 标题页文字是否在全屏下位置自然。
- 左上测绘表是否挡住关键物件。
- 对话框是否挡住交互提示或关键显影。
- 拍照取景框是否能被明确看见。
- 结尾卡是否居中、无文字溢出。

当前记录：

- `[fixed]` 标题页正文在 4K 全屏截图下曾出现不自然断行，已修复。
- `[pass]` 标题页和开场对话在默认 960x540 逻辑视口下未发现明显文字溢出。
- `[pass]` 开场对话框在 4K 全屏截图下未发现明显文字溢出。
- `[pass]` A2 显影层在默认逻辑视口下可见，完成纸签也能明确出现。
- `[pass]` A5 拍照取景框、快门闪光和“测绘照片记录”字幕在默认逻辑视口下可见；当前闪光偏强，但能解决“玩家感知不到”的问题。
- `[pass]` A12 终局显影、对话框和最终画面层次在默认逻辑视口下可见。
- `[fixed]` 左上测绘表曾在 A2/A12 截图中视觉占比偏大；现已在拍照/显影期间自动弱化，交互结束后恢复。
- `[partial]` 宽屏比例仍需人工或 OS 级截图确认；当前脚本只能稳定捕获 960x540 逻辑视口。
- `[pending]` 仍需人工实际操作检查 A1-A12 全流程中的 UI 遮挡。

### 动线与交互

状态：`[pending]`

- A1 是否容易找到。
- A2/A3/A4/A5 第一批调查是否有足够指引。
- A6/A7/A8/A9/A10 的空间回看是否清楚。
- A11 作文本碎页是否容易漏掉。
- A12 最后一张照片是否容易找到。

### 表现与节奏

状态：`[partial]`

- 拍照反馈是否足够明显。
- 显影层是否足够明显。
- 完成纸签是否有“已记录”的感觉。
- 对话最小停顿是否拖慢流程。
- 程序化音频是否刺耳、突兀或压过文本。

当前记录：

- `[pass]` 自动烟测确认真实触发路径下拍照反馈和显影层会进入可见状态。
- `[pass]` 截图确认 A2/A12 显影层、A5 拍照取景框和快门闪光能被看见。
- `[pass]` 自动烟测确认拍照/显影期间测绘表会弱化，避免抢占关键表现注意力。
- `[pending]` 程序化音频仍需人工听感确认。

### 文本与情绪

状态：`[pending]`

- 开场地名钩子是否清楚。
- A7 对讲机打断是否成立。
- A8 父亲记录是否自然回收钩子。
- A10 朋友去向是否过渡自然。
- A11 何小满线索是否在文本中成立。
- A12 结尾是否收得住。

## 问题记录

当前暂无人工手测记录。

后续格式：

```text
- [severity: high|medium|low] 所属步骤：问题描述。建议处理方式。
```

## 通过标准

P0.1 可以标记为 `[done]` 的条件：

- 至少完成一次不使用 F6/F7 的完整手测。
- 所有 high 问题已修复或降级为已知限制。
- medium/low 问题已记录并排入 P0/P1/P2。
