# 《最后一间教室》v0.1 后续事件路线图

本文把对话中确认的后续待做事件正式落到项目文件中。后续推进时以本文为主，不只依赖对话上下文。

状态说明：

- `[done]` 已完成并验证。
- `[doing]` 当前优先推进。
- `[todo]` 尚未开始。
- `[blocked]` 需要外部条件或新的设计决策。

## 当前阶段

当前目标：把 v0.1 从“内部可展示原型”推进到“可稳定展示、可手动验收、后续可扩展”的小发布前状态。

最新基线：

- 标题入口已完成。
- A1-A12 主线闭环已完成。
- 生成资产第一轮接入已完成。
- 拍照反馈和显影层已调到更可感知。
- 自动验证包含标题页、主流程、视觉反馈、可达性、音频安全、显示配置六条烟测。

## P0：先保证原型可稳定展示

### P0.1 完整手动游玩 QA

状态：`[done]`

目标：

- 从标题页开始，不使用调试键，完整跑通 A1-A12。
- 记录实际游玩中的 UI 遮挡、交互点难找、反馈不明显、文本节奏问题。

验收：

- 能按正常流程完成 A12 并看到结尾卡。
- 没有必须使用 F6/F7 才能继续的卡点。
- 问题被记录到 `docs/qa/manual-playtest-notes-v0.1.zh.md`。

当前进度：

- 已创建手动游玩记录文件。
- 已记录当前自动验证基线。
- 已新增截图捕获脚本并完成标题页/开场对话的 4K 全屏截图检查。
- 已补充 A2 显影、A5 拍照反馈、A12 终局显影的默认逻辑视口截图检查。
- 已新增可达性烟测，确认 A1-A12 在玩家进入交互热区后可通过真实触发路径完成。
- 已修复调查点未明确检测玩家 collision layer 的问题。
- 已增强交互提示，靠近物件时显示“动作：物件名”，降低 A6/A9/A11 等后半段调查点的漏看风险。
- 已增强目标文案的空间方向提示，并由可达性烟测确认目标与下一步调查点一致。
- 已新增音频安全烟测，确认程序化音频峰值、RMS 和默认音量低于安全阈值。
- 已新增手动验收清单 `docs/qa/manual-playtest-checklist-v0.1.zh.md`，用于完整人工手测签收。
- 2026-05-06 已完成一次不使用调试跳关的人工完整手测，全部通过。

### P0.2 全屏与不同窗口比例检查

状态：`[doing]`

目标：

- 检查标题页、对话框、测绘表、拍照取景框、结尾卡在全屏和窗口模式下是否错位或遮挡。

验收：

- 至少检查默认 960x540、全屏、一个更宽窗口比例。
- 发现的问题记录到手动 QA 文档。

当前进度：

- 已完成 4K 全屏下标题页和开场对话截图检查。
- 已完成默认 960x540 逻辑视口下标题页和开场对话截图检查。
- 已完成默认 960x540 逻辑视口下 A2/A5/A12 关键表现状态截图检查。
- 已修复标题页正文不自然断行。
- 已设置 `window/stretch/aspect="keep"`，全屏或宽屏窗口下保持 16:9 设计比例，避免 UI/场景拉伸变形。
- 已新增显示配置烟测，固定检查全屏、960x540 设计视口、`canvas_items` 和 `keep` 比例保护。
- 宽屏截图脚本尝试后发现当前 Godot 逻辑视口仍固定输出 960x540；需要人工或 OS 级截图补充真实窗口比例检查。

### P0.3 表现层二次核实

状态：`[done]`

目标：

- 核实拍照反馈、显影层、完成纸签、音效是否能被玩家感知。

已完成：

- 新增 `visual_feedback_smoke_test.gd`。
- 拍照取景框、快门闪光、显影层透明度和停留时间已增强。

验收：

- `VISUAL_FEEDBACK_SMOKE_TEST_OK` 通过。

## P1：提升展示完成度

### P1.1 墙面关键物件资产补齐

状态：`[todo]`

目标：

- 用统一风格资产替换或增强奖状、撤并通知、旧校名牌/地图。
- 提升教室作为“真实存在过的空间”的信息密度。

验收：

- A4/A5/A6 视觉锚点比当前占位更清楚。
- 不改变既有 A4/A5/A6 交互顺序和剧情文本。

### P1.2 A11 何小满线索轻量加强

状态：`[todo]`

目标：

- 保持原型阶段的克制，不做人物登场。
- 只在文本、作文本线索或显影文案中确保“何小满出现”足够自然。

验收：

- 玩家能从 A11 文本理解何小满是谁、她代表什么。
- 不需要新增人物立绘。

### P1.3 真实音频替换第一批

状态：`[todo]`

目标：

- 替换程序化占位音频中最影响展示质感的部分。

优先项：

- 教室环境底噪。
- 快门声。
- 旧纸/档案柜声。

验收：

- 音频不恐怖、不煽情、不压过文本。
- 支持后续音量设置。

## P2：让 demo 更像可发布版本

### P2.1 最小设置菜单

状态：`[todo]`

目标：

- 提供音量、窗口/全屏、退出等基础控制。

验收：

- 玩家能在不关闭程序的情况下调整音量。
- 入口或游戏内能返回标题/退出。

### P2.2 导出构建

状态：`[todo]`

目标：

- 先做 macOS 本机可运行包。
- 后续再考虑 Windows/Web。

验收：

- 导出包能从标题页进入并完成 A1-A12。

### P2.3 标题页/结尾页轻微打磨

状态：`[todo]`

目标：

- 增加更自然的淡入、版本信息、返回标题能力。

验收：

- 不新增复杂菜单前提下，展示入口和结尾收束更完整。

## P3：下一阶段内容扩展

### P3.1 人物插图预留接口落地

状态：`[todo]`

目标：

- 不急着制作正式立绘。
- 先把 portrait 机制做得可替换。

验收：

- 小李、老李、何小满、母亲可以在数据中配置 portrait id。
- 没有 portrait 时保持当前文本体验。

### P3.2 资产生成流程标准化

状态：`[todo]`

目标：

- 把 prompt、原图、裁剪、降噪、接入、校准流程整理成可复用 pipeline。

验收：

- 后续新资产能按同一流程从生成到接入。

### P3.3 v0.2 剧情/玩法扩展规划

状态：`[todo]`

目标：

- 决定是否扩展第二个空间、更多调查物、或更完整的父亲/何小满线索。

验收：

- 在 v0.1 展示稳定后再启动，不抢当前封版工作。
